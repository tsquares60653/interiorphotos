import React from 'react';
import ReactDOM from 'react-dom/client';
import VirtualGalleryWalk from './VirtualGalleryWalk';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <VirtualGalleryWalk />
  </React.StrictMode>
);
